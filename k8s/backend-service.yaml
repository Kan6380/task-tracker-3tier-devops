apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
  # No "type" = ClusterIP: only the frontend (inside the cluster) can
  # reach this. Nothing outside the cluster can hit the API directly.
